We have migrated the sMAP source to [GitHub](https://github.com/SoftwareDefinedBuildings/smap).  You can get the latest copy of sMAP using git:

```
$ git clone git@github.com:SoftwareDefinedBuildings/smap.git
```